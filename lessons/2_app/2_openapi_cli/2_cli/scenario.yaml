steps:
  - name: "List items"
    method: GET
    path: /items
    expect_status: 200
    description: "GET /items — приложение запущено и отвечает"

  - name: "Create item"
    method: POST
    path: /items
    body:
      id: null
      name: "Test Item"
    expect_status: 201
    description: "POST /items создаёт элемент"
    save_as: item

  - name: "Get by ID"
    method: GET
    path: "/items/${item.id}"
    expect_status: 200
    description: "Маршруты зарегистрированы через Router — все работают"

  - name: "Delete"
    method: DELETE
    path: "/items/${item.id}"
    expect_status: 204
    description: "DELETE /items/{id} возвращает 204"
