project:
  root_dir: "/home/e.mesheryakou/other/learn_litestar"
  source_dir: "src"
  tests_dir: "tests"
  docs_dir: "docs"

scanner:
  # Directories to completely exclude from scanning
  exclude_dirs:
    - ".git"
    - ".venv"
    - "venv"
    - "__pycache__"
    - "node_modules"
    - "migrations"
    - "build"
    - "dist"
    - ".idea"
    - ".vscode"

  # Specific files to ignore during analysis
  ignore_files:
    - "conftest.py"
    - "manage.py"
    - "setup.py"
    - "__main__.py"